# S4 Gate Pack — internal gates for Assemble + Taste Onboarding (2026-07-19)

Runner: orchestrator-side gate agent (Claude Fable 5). Standard:
`S3-GATE-lane-b.md` — honest method, honest limits. Both gates per
`SPRINT-4-D4.md` §Internal gates. Nothing in this record is simulated;
every number below was measured on this machine, and every failure is
reported as found. **Repo code untouched** — gates measure, fixes go to
the queue.

Environment: repo venv (CPython 3.14.6) + ffmpeg 8.1.1 (gyan.dev full
build). Installed during the gate, documented as env prep: `pysubs2
1.8.1` (the `[render]` extra was missing from the venv — `zing render`
failed honestly until installed), `OpenTimelineIO 0.18.1`, `pytest
9.1.1`, and `deno 2.9.3` (scratchpad-only binary, see Gate 2 §fetch).
Workspaces (session scratchpad): `s4-gate-home` (Track 1, seeded per the
S3 record's method), `s4-setup-home` (Track 2, created clean).

---

## GATE 1 (Track 1): direction → draft EDL → render → exports

### Method

1. **Seeded workspace** rebuilt from the S3 gate workspace (found intact
   in the Lane B session scratchpad): `raw-editing-practice` breakdown
   WITH the S3 run's `judgment["direct"]` (`_meta`: prompt 1.0.0,
   claude-fable-5, 2026-07-19T03:29Z) and its `direction.md`, plus the
   `gate-refs-v2` profile. Keepers: 0.0–13.4s (hook+setup) and
   45.0–48.3s (payoff line).
2. **Media**: the frozen fixture commits no media. Used the re-fetch in
   Lane A's raw-check scratchpad (sha256 `078f593d…e290009`, 31.8 MB
   1080p variant — NOT byte-identical to the fixture's 8.6 MB source,
   sha `0450698e…459d031`). Alignment verified before use: duration
   55.074s exact, 1920x1080@30 h264+aac matching frozen meta, and
   frames at 1s and 46s visually matched against the fixture's
   committed `hook_1s.jpg` / `shot_030.jpg` (same content, higher res).
3. **Assemble path (real)**: `myzing.assemble.draft.draft_for_slug`
   ("raw-editing-practice", the S3 direction). There is **no CLI or MCP
   surface** for it (defect D-7) — driven via Python, the only way that
   exists.
4. **Captions**: no production path derives `CaptionSpec`s from a
   breakdown (defect D-8), so gate glue remapped the breakdown's
   measured `words[]` inside each keeper span onto the draft timeline
   (4-word word-timed chunks, `lower`, ALL-CAPS). Documented manual
   step, not hidden.
5. **VO**: attempted for real, then skipped honestly (below). No fake
   audio.
6. **Render**: `zing render draft-edl-captioned.json -o draft.mp4
   --keep-work work --otio draft.otio` → exit 0 in ~6s, with 9 honest
   CPS warnings (my 4-word chunks read up to 35 chars/s — glue artifact,
   not renderer fault).

### Evidence

**Assemble** — 2 clips, contiguous (0→13.4→16.7s), 1920x1080@30 from
measured meta; honest warning emitted: *"study ran without raw mode, so
chosen spans could not be cross-checked against measured keepers"* —
exactly the S3 caveat carried forward. `draft-edl.json` +
`draft-edl-captioned.json` (16 caption chunks) written next to the
breakdown.

**VO (honest skip)** — `zing render … --voiceover-script vo-line.txt`
(shot prompt 1's line) failed loud, exit 1: *"Kokoro model assets are
missing: …kokoro-v1.0.onnx, …voices-v1.0.bin … Zing never downloads
models during a render."* Model files exist nowhere on this machine;
`kokoro-onnx` is not installed and cannot be (repo venv is Python
3.14.6; the package supports 3.10–3.13 per its own error text). The
"with VO" half of the Track 1 gate is therefore **not exercised** —
stated, not papered. Note defect D-6: `zing doctor`'s fix-hint claims
render fetches the model on first VO use; tts.py refuses by design.

**ffprobe (rendered draft)** — duration 16.700000 exactly (= 13.4 +
3.3); h264 1920x1080 yuv420p 30/1 + aac 48000 Hz stereo. Matches the
EDL.

**Captions (renderer's own probe approach)** — `captions.ass` from
`--keep-work`: 64 word-pop Dialogue events, Karaoke style, `\pos(960,810)`
= `caption_anchor("lower", 1920, 1080)`. Pixel probe per the
integration-test oracle (gray region 400x120 at the anchor, caption-on
t=1.0 vs caption-off t=13.8 inside the computed 13.32–14.28s gap): abs
gray diff **5,574,374** vs the test's >5,000 bar. Visually confirmed in
extracted frames ("ANYWHERE" word-pop at 0.6s; yellow karaoke "TO" at
14.8s).

**OTIO** — `draft.otio` parses with opentimelineio 0.18.1: Timeline
"draft", Video track with 2 clips (frames 0–402 = 0–13.4s; 1350–1449 =
45.0–48.3s @30fps), mirrored Source Audio track, absolute file-URL media
references, JSON `OTIO_SCHEMA: Timeline.1`. Correct mapping of both
keepers.

**Audio — the gate's real find.** Windowed volumedetect on the draft:
0–13.4s speech at −21 dB mean… then **13.4–16.7s at −91 dB: the payoff
line is silent.** And 0–3.3s is anomalously hot (−16.5 dB vs −24 dB
source): keeper 2's speech is **mixed under the opening** instead of
placed at 13.4s. Isolated stepwise with ffmpeg (all repro commands in
the gate scratchpad):

- source media HAS speech at 45–48.3s (−17.8 dB) — not a media problem;
- the exact saved filtergraph reproduces the fault; clip-audio chain
  alone (`…adelay=13400:all=1` → WAV) yields a **3.34s** file — adelay's
  inserted silence never survives;
- `ashowinfo` after adelay: first frame `pts_time:-192153584101141` —
  the inserted silence carries garbage negative PTS on ffmpeg 8.1.1, so
  the chain's trailing `atrim=duration=16.7` (and amix's PTS alignment)
  discards it;
- **verified one-filter fix**: `asetpts=PTS-STARTPTS` immediately after
  `adelay` → silence 0–13.4s, speech lands 13.4–16.7s, 16.7s total.
- All 6 `test_render_integration.py` tests **pass** on this same
  ffmpeg — no test asserts per-clip audio placement (defect D-2).

**Watchable?** — **No.** What a viewer sees: a 16.7s vertical-ish
landscape draft that opens on the hook ("I don't go anywhere without a
watch") with clean word-pop captions, hard-cuts at 13.4s to the watch
close-up and the payoff beat — visually coherent, captions land. What a
viewer *hears*: two takes talking over each other for the first 3.3
seconds, then the payoff line playing **mute** while its captions pop.
Also visible: the source's own burned-in captions under Zing's (the
F-16 stand-in is an edited showcase; expected). This is not a draft you
could hand a creator.

### Verdict (per criterion)

| Criterion | Verdict |
|---|---|
| Direction output (judgment["direct"] + keepers) present with S3 provenance | PASS |
| Draft EDL via the assemble path, honest warnings | PASS |
| VO via local Kokoro | HONEST SKIP (model absent; loud failure captured; no fake audio) |
| Render with captions — probe-verified | PASS on captions/duration/resolution/streams |
| Render audio correctness | **FAIL** (D-1: second clip's audio misplaced + silent tail) |
| .otio export parses, correct structure | PASS |
| **Watchable draft end-to-end** | **FAIL** — chain proven, output audio-defective |

**Gate 1: FAIL** on the headline criterion, with the failing stage
isolated to one queued one-filter fix (D-1). Every other link in the
chain (direction → EDL → captions → render → OTIO) demonstrated with
real artifacts.

---

## GATE 2 (Track 2): fresh workspace → zing setup → two profiles → compare

### Method + evidence, in run order

1. **Clean `ZING_HOME`** (`s4-setup-home`, empty). `zing setup --list`:
   5 packs listed (ai-tech-talking-head 7 refs, informative-explainer
   5, product-launch 6, viral-tiktok-reels 7, vlog 7).
2. **`zing setup --pack ai-tech-talking-head`** (one-shot CLI, cold):
   printed 7× `[running]`, "Started 7 new studies in the background",
   exit 3 in **0.35s** — and the process exit killed its own daemon
   study threads. Statuses wedge at `running` (dead pid 49212);
   re-running setup restarts **nothing** (`plan_setup` only starts
   `unstudied`) and would print "studies in progress" forever. **The
   one-shot CLI cannot ever complete a cold onboarding** (defects D-3,
   D-4).
3. **Fetch layer hard-blocked.** Recovery driven the MCP way in one
   persistent process (the flow `setup_flow` documents): `zing_status()`
   F-03-reconciled all 7 to `failed` with the exact honest message
   ("runner process (pid 49212) is dead; call study_video again"), and
   `study_video` restarts worked — but every yt-dlp fetch failed:
   the S2 warning ("No supported JavaScript runtime… deprecated") is now
   **fatal** on yt-dlp 2026.07.04 (current). With deno 2.9.3 supplied,
   YouTube then **bot-gates this IP outright** ("Sign in to confirm
   you're not a bot") on every player client tested (web, android, ios,
   tv, tv_embedded, web_embedded, mweb). Browser-cookie workarounds were
   NOT used. Fresh network fetches: blocked, full stop (defect D-9).
4. **Reuse path (the flow's own design: "starts studies only for what's
   missing").** Seeded the fresh workspace with breakdowns studied FOR
   REAL on this machine before the block: all 7 pack references from
   Lane A's packs-ws (measured 2026-07-19T03:47Z, provenance intact) and
   the 2 personal links from the S2 gate workspace (`~/.zing`, measured
   2026-07-18). No measurements fabricated; provenance travels with the
   files.
5. **`zing setup --pack ai-tech-talking-head`** again: builds
   synchronously, exit 0, honest tail: *"built from 7 references (7
   awaiting judgment)"*. **`zing setup --links <2 gate-pack URLs>
   --name my-taste --genre talking-head --platform youtube`**: exit 0,
   "built from 2 references (2 awaiting judgment)".
6. **Provenance verified (profile.json, not the pretty-printer):**
   - `my-taste`: `judged: {}`, `unjudged_source_slugs` = both slugs —
     **unjudged-flagged as required**; honest music-rate warning (n=1
     conclusive of 2).
   - `ai-tech-talking-head` (setup-built): unjudged-flagged, but **NO
     `preset_pack` provenance** — no pack_id, no manifest sha, no
     per-reference outcomes. The setup path loses the truth-hash lesson
     (defect D-5).
   - `pack-ai-tech-talking-head` via **`zing profile pack
     presets/ai-tech-talking-head.json`** (Lane A's builder, run in the
     same workspace): "0 studied, 7 reused, 0 failed";
     `provenance.preset_pack` = pack_id, `manifest_sha256 b8fdb69f…`,
     curated_at 2026-07-19, per-reference outcomes all `reused`,
     rebuilt_at stamp. **Pack-manifest provenance: present and honest**
     on the builder path.
   - Both pack-built profiles emit their own coherence warning: source
     durations 37.8–374.4s, >3× spread — the pack pools 5 vertical
     shorts with 2 horizontal 5–6min videos (observation O-1).
7. **Compare-judgment.** Verified runnable against each: profile +
   compare prompt (0.5.1) + candidate breakdown all load for
   `my-taste`; and against the preset profile one judgment was **run
   for real, by me, following `prompts/compare.md` 0.5.1**:
   `youtube-n13pfpa5ddi` (Cleo, 72.609s) vs `pack-ai-tech-talking-head`
   (n=7). Headline: **strong_fit with the target blurry exactly where
   the video deviates** — 4/6 fit dimensions inside band (duration,
   pacing 8/10 curve buckets, hook dead-center on the pack's unanimous
   zero-width 0.0s first-word band, audio), captions near_band (0.981
   ALL-CAPS vs pooled 0.26 — per-source computation showed the pool is
   dominated by the two sentence-case horizontal long-forms:
   0.93/0.24/0.49/0.54/0.44 vertical vs 0.11/0.21 horizontal),
   transitions cannot_judge (**profile side n=0** this time — pack refs
   studied without the detector). Rubric G-TH-1..7: 5,5,4,4,4,4,5 with
   keyframe evidence. Saved via `save_judgment(section="compare",
   model="claude-fable-5")` → contract-validated, `prompt_version
   0.5.1` stamped onto the breakdown.

### Verdict (per criterion)

| Criterion | Verdict |
|---|---|
| List presets | PASS (5 packs) |
| Cold `zing setup --pack` on a fresh workspace (one-shot CLI) | **FAIL** (D-3 wedge; D-4 no restart of failed) |
| Same flow, studies present (reuse path — the design's own idempotent half) | PASS (exit 0, honest output) |
| Paste-your-own-links path, 2 gate-pack URLs | PASS via reuse (fresh fetch blocked — see limits) |
| Preset profile with pack-manifest provenance | PASS via `zing profile pack`; **FAIL via `zing setup --pack`** (D-5) |
| Personal profile unjudged-flagged | PASS |
| Compare-judgment runnable against each; one run for real | PASS (full judgment vs preset; readiness vs personal) |

**Gate 2: PASS on mechanics with two P1-adjacent defects** (cold-start
wedge; provenance loss on the setup path) **and a hard external
blocker** on fresh fetches.

---

## Honest limits of this gate pack

1. **No VO was rendered anywhere** — Kokoro model absent, runtime
   uninstallable on the venv's Python 3.14. Track 1's "with VO" clause
   is unexercised. Nothing was faked to cover it.
2. **Gate 1's media is a re-fetch**, not the frozen fixture's bytes
   (sha256s differ; 1080p vs the fixture's smaller format). Alignment
   was verified by duration/streams/two-frame visual match, not by hash.
3. **The direction was reused from the S3 run**, not re-judged; all S3
   caveats (F-16 edited stand-in, no raw-mode keepers, loose gate
   profile) carry forward into this draft.
4. **Gate 2's fresh-fetch half is unproven**: YouTube bot-gating blocked
   every fetch during the gate window. The cold-network onboarding claim
   ("fresh machine, seven URLs, one command") remains untested until the
   network path clears (or cookies/PO-token support is a sanctioned
   decision — it was not mine to take).
5. **One full compare, not two** — the personal-profile compare was
   readiness-verified only.
6. **All profiles are measured-only** (every source unjudged) — the
   judged half of profile-grounded comparison is still exercised as
   "honestly empty", same as S2.
7. Caption CPS warnings in Gate 1 are artifacts of my 4-word glue
   chunking; renderer guidance flagged them correctly.

## Defects found (queue-ready)

- **D-1 (Lane C, P1):** render graph — `adelay`'s inserted silence
  carries garbage negative PTS on ffmpeg 8.1.1, so the clip-audio
  chain's trailing `atrim=duration=` drops it: every clip with
  `timeline_start>0` mixes its audio at t=0 and the output tail goes
  silent. Repro: any 2-clip EDL + windowed volumedetect. Verified fix:
  `asetpts=PTS-STARTPTS` immediately after `adelay` in graph.py.
- **D-2 (Lane C):** all render integration tests pass while D-1 ships —
  no test asserts per-clip audio placement; add windowed level probes
  per clip slot (distinct tones per clip).
- **D-3 (Lane B, P1):** one-shot `zing setup` can never finish a cold
  onboarding — `h_study_video` daemon threads die with the CLI process
  (<1s), statuses wedge at `running`, and re-runs start nothing. The
  CLI needs to either wait on its own jobs or spawn detached workers.
- **D-4 (Lane B):** `advance_setup` never restarts `failed` studies —
  after F-03 reconcile, `zing setup` parks forever; the reconcile
  message says "call study_video again" but setup never does.
- **D-5 (Lane B/A):** `zing setup --pack` builds the preset profile
  without the pack's manifest provenance (no pack_id/manifest_sha/
  per-ref outcomes) — route it through `build_pack` or stamp
  `preset_pack` equivalently.
- **D-6 (Lane B):** `zing doctor` tts fix-hint promises "zing render
  fetches the kokoro model on first VO use"; tts.py by design never
  downloads — hint lies, fix the wording (and decide who fetches).
- **D-7 (Lane A):** the assemble path (`draft_for_slug`) has no CLI or
  MCP surface — Track 1's direction→draft step is unreachable outside
  Python imports.
- **D-8 (Lane A/C):** no production path derives caption specs from a
  breakdown's measured words — draft EDLs render caption-less without
  hand glue; the S4 "captions from measurements" step needs an owner.
- **D-9 (Lane B, env):** yt-dlp without a JS runtime is now FATAL for
  YouTube (S2's warning came true); doctor should fail-or-warn on
  missing deno AND surface bot-gate errors distinctly. Related: B-Q12
  (yt-dlp not in `[study]` extras) still open.
- **O-1 (Lane A, observation):** the ai-tech-talking-head pack pools
  vertical shorts and horizontal long-forms into one profile — its own
  coherence warning fires and pooled caption stats are dominated by the
  long-forms; consider per-orientation variant packs (A-Q14 wording
  already allows it).

## Bottom line

Track 1's chain is real end-to-end — direction to EDL to captioned
render to parsing OTIO — and the gate caught a genuine P1 audio
placement bug (with a verified one-filter fix) that six green
integration tests missed. Track 2's onboarding machinery, provenance
honesty, and profile-grounded compare all work; its cold-start CLI and
its provenance-carrying setup path do not, and YouTube currently blocks
the fetch layer entirely. Neither gate was passed by pretending.

---

## Gate 1 RERUN 2026-07-19

Runner: orchestrator-side gate agent (Claude Fable 5), after the defect
fixes landed on main. Confirmed merged before the run: **#150**
(`a75734c`, render: preserve delayed clip audio timestamps — the D-1
`asetpts=PTS-STARTPTS` fix plus a D-2 per-slot tone-placement
regression test), **#149** (`b2c12ac`, D-7 assemble CLI + D-8 measured
captions), **#148** (`bc8ee71`, D-3/D-4/D-5/D-6/D-9-doctor), **#152**
(`f9ad8f4`, full-range JPEG extraction). Same environment as the failed
run: repo venv (CPython 3.14.6, myzing editable from main), ffmpeg
8.1.1 gyan full build, pysubs2 1.8.1, OpenTimelineIO 0.18.1. **No
fresh YouTube fetches** — cached/local media only (bot-gating per D-9
still stands).

### Method delta vs the failed run

1. **Workspace**: fresh `s4-gate1-rerun-home`, seeded from the intact
   `s4-gate-home` exactly as step 1 seeded from S3: same
   `breakdown.json` WITH `judgment["direct"]` (`_meta`: prompt 1.0.0,
   claude-fable-5, 2026-07-19T03:29Z), same `direction.md`, same
   `gate-refs-v2` profile. Keepers unchanged: 0.0–13.4s and 45.0–48.3s.
2. **Media**: the SAME cached file, byte-identical — sha256 verified
   `078f593d392ba54a8558febc1c3d4e30779fdcae87be8a6dc0a86bdc0e290009`,
   the exact hash the failed record cited. Nothing fetched.
3. **Assemble — no more Python glue (D-7 fixed)**: `zing assemble
   raw-editing-practice --direction direction.json` (direction JSON =
   `judgment["direct"]` extracted verbatim from the breakdown) → exit
   0, "2 clip(s), 16.7s, 14 caption(s), 1920x1080", same honest
   raw-mode warning as before. EDL maps both keepers correctly (clip 2
   at `timeline_start` 13.4).
4. **Captions — no more hand glue (D-8 fixed)**: the draft EDL now
   carries 14 word-timed `CaptionSpec`s derived from the breakdown's
   measured `words[]`, style MEASURED from the source's own captions
   (center, sentence-case, word-timed) instead of the failed run's
   glued lower/ALL-CAPS 4-word chunks. One render of `draft-edl.json`
   directly — no `draft-edl-captioned.json` step exists anymore.
5. **VO**: attempted for real, failed loud again, exit 1: Kokoro model
   assets still missing (`~/.cache/myzing/kokoro/`), "Zing never
   downloads models during a render." kokoro-onnx still uninstallable
   on the venv's Python 3.14. **Honest skip, unchanged.** (The failed
   attempt leaves an empty `draft-vo-assets/` dir behind — cosmetic,
   noted below.)
6. **Render**: `zing render draft-edl.json -o draft.mp4 --keep-work
   work --otio draft.otio` → exit 0 in ~5.5s, 7 honest CPS warnings
   (now from real measured word timings — e.g. captions[5] reads at
   43.8 cps — not glue artifacts).

### Probe evidence — the original failure mode, retested

Renderer's own RMS-window method (`atrim` window → `asetpts` →
`volumedetect`), at the exact timestamps the failed record cited:

| Window | Failed run | RERUN | Source (same span) |
|---|---|---|---|
| draft 13.4–16.7s (clip 2 slot / payoff) | **−91 dB — silent** | **−17.8 dB mean, −1.0 dB max** | −17.8 / −0.9 dB (45.0–48.3s) |
| draft 0–3.3s (opening) | **−16.5 dB — keeper 2 mixed under it** | **−23.3 dB mean** | −23.3 dB (0–3.3s) |
| draft 15.5–16.7s (the tail itself) | (silent) | **−18.5 dB mean** | −18.5 dB (47.1–48.3s) |
| draft 0–13.4s (clip 1 slot) | −21 dB (inflated by the double-mix) | −24.1 dB | — |

Clip 2's audio now sits in clip 2's slot at source level to within
0.1 dB, the opening is no longer double-mixed, and the payoff line is
audible to the last frame. **D-1's failure mode is gone.** Corroboration:
all 9 tests in `test_render_integration.py` + `test_render_graph.py`
pass on this same ffmpeg 8.1.1, including #150's new
`test_clip_audio_follows_each_timeline_slot` (the D-2 answer — per-slot
bandpassed tone probes, ≥20 dB isolation asserted).

**Other probes** — ffprobe: duration 16.700000 exact, h264
1920x1080@30 yuv420p + aac 48 kHz stereo. Captions: `captions.ass` has
64 word-pop Karaoke Dialogue events at `\pos(960,540)` =
`caption_anchor("center", 1920, 1080)`; pixel probe per the
integration-test oracle (gray 400x120 at the anchor): on/off diff
**4,953,368** (t=1.0 vs t=13.8 in the computed 13.32–14.28s gap), and
a cleaner same-scene variant inside clip 2 (t=14.8 on vs t=13.8 off)
gives **4,830,348** — captions land, visually confirmed in extracted
frames (yellow karaoke "anywhere" at 1.0s; "to" over the watch
close-up at 14.8s). **OTIO**: `draft.otio` parses with opentimelineio
0.18.1 — Timeline "draft", Video + mirrored Audio tracks, clip 1
frames 0–402, clip 2 frames 1350–1449 @30fps. Same correct mapping.

**Watchable? — Yes, as a draft.** What a viewer gets: a 16.7s
landscape draft that opens on the hook, cuts hard at 13.4s to the
watch close-up, and the payoff line now PLAYS in its slot — one voice
at a time, audible to the end, word-timed captions popping in the
source's own measured style. Remaining blemishes, all stated: the F-16
stand-in's burned-in captions/"Raw Video Preview" framing show under
Zing's captions (source-material issue, carried forward from S3, not a
renderer fault); 7 caption chunks exceed the 20 cps guidance (fast
real speech, correctly warned); and the payoff's final word is
uncaptioned (D-10, below). This is now a draft you could hand a
creator — with the stand-in-source caveat.

### Verdict (per criterion)

| Criterion | Failed run | RERUN |
|---|---|---|
| Direction output present with S3 provenance | PASS | PASS (same artifact, reused) |
| Draft EDL via the assemble path | PASS (Python-only, D-7) | **PASS via `zing assemble` CLI** — D-7 fix works |
| Captions from measurements | manual glue (D-8) | **PASS via production path** — D-8 fix works |
| VO via local Kokoro | HONEST SKIP | HONEST SKIP (model still absent; loud failure; no fake audio) |
| Render — captions/duration/resolution/streams | PASS | PASS |
| Render audio correctness | **FAIL (D-1)** | **PASS** — probe-verified at the failed run's exact windows |
| .otio export parses, correct structure | PASS | PASS |
| **Watchable draft end-to-end** | **FAIL** | **PASS** (stand-in-source blemishes stated) |

**Gate 1 RERUN: PASS.** The one failing criterion from 2026-07-19 is
fixed and probe-verified; every other link re-demonstrated on the
production surfaces that now exist for it.

### New defects (queue-ready)

- **D-10 (Lane A, P3):** caption derivation drops trim-edge words —
  `_caption_specs` keeps only words with `w.end <= clip.src_out`, so
  the payoff's final word "this" (measured 48.02–48.32s vs trim at
  48.3s) is 93% audible in the render but never captioned; the draft's
  captions end at 16.42s while speech runs to 16.7s. Fix shape: admit
  words whose midpoint lies inside the span and clamp start/end to the
  trim (mirror at `src_in`).
- **O-2 (Lane C, observation):** a VO render that fails at TTS setup
  leaves an empty `<output>-assets/` directory behind. Cosmetic;
  clean up on failure.

### Honest limits of the rerun

Unchanged from the failed run where not fixed: no VO rendered anywhere
(model absent, runtime uninstallable on Python 3.14 — the "with VO"
clause is still unexercised); media is the same verified re-fetch, not
the frozen fixture's bytes; the direction is the S3-era judgment,
reused not re-judged, with all its caveats (F-16 edited stand-in, no
raw-mode keepers, loose gate profile). The caption style being
"center/sentence-case" is honestly MEASURED — from the stand-in's own
burned captions, which is what D-8's rule says to do with this input.
