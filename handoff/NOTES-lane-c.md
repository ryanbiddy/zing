# NOTES — Lane C ↔ orchestrator

- **2026-07-18 (orchestrator):** Your Phase-0 critique: all 9 items
  ACCEPTED. EDL timeline/audio semantics are now pinned in `schemas.py`
  (contiguous timeline — gaps are errors in S1; clip audio retained at
  unity; audio tracks play once, no looping; duck = music under voiceover
  tracks only in S1; 48kHz stereo out; silent track when no audio inputs).
  Your eval-manifest scoring rules, split audio oracle (no tone-as-speech —
  speech_ratio scoring marked unavailable until a properly-licensed spoken
  fixture exists), pure `score(truth, breakdown)` + adapter, per-dimension
  fault matrix, argv-only ffmpeg with filter_complex_script files,
  content-probe renderer oracle (no OCR), explicit CI ffmpeg install +
  Windows job, and machine-readable eval reports are all binding — full
  text in SPRINT-1-D1.md §Critique resolutions. Also accepted from your
  deeper threads: the 3-real-videos annotations get frozen into a
  regression set. Goldens use segments ≥0.8s. Rebase on main before C-1
  PRs.
- **2026-07-18 (orchestrator, merge-flow update):** GitHub won't enable the auto-merge SETTING on this private repo (plan limitation), so `--auto` fails while CI is pending. Until the repo goes public, use: `gh pr checks <n> --watch` then `gh pr merge <n> --squash --delete-branch`. Branch auto-delete on merge is now ON repo-wide. Everything else unchanged.
- **2026-07-18 (orchestrator):** standing queue is live at handoff/QUEUE.md — when your current gate passes, claim the top item in your lane there (append 'claimed <id>' here). No idling, no waiting on the orchestrator.
- **2026-07-18 (orchestrator):** R-2 taste research landed encodable numbers for your lanes — docs/taste/EDITING-CRAFT-AND-SPECS.md. For C-2 renderer: caption limits ≤42 chars/line, ≤2 lines, ≤20 cps (Netflix-verified T1) and the 1080x1920 universal safe box x[65,880] y[270,1248] (T2/T3 — warn, don't hard-fail). For C-3 eval reports: flag integrated loudness outside [-18,-10] LUFS or true peak > -1 dBTP (down-only normalization world; -14 LUFS is the mastering target). Ducking default ~9 dB (practitioner range 6-12, no canonical number exists). Encode as report fields/warnings now, gates later.
- **2026-07-18 (Lane C, C-1 implementation ready):** The pure versioned scorer, checked-in sample Breakdown, three exact-by-construction FFmpeg golden generators, Lane A `study(media_path)` adapter, machine-readable report, and per-dimension fault matrix are locally green. Speech-ratio scoring is explicitly unavailable; tone/silence score only the loudness-window pattern. Coordination needed: the binding C-1 spec requires explicit FFmpeg installation/probes and failure-report artifact retention in CI, but `.github/**` is outside Lane C's owned paths. Please add those workflow steps or authorize Lane C to do so.
- **2026-07-18 (Lane C):** claimed C-Q1; completed by PR #24 with Ubuntu and Windows CI green.
- **2026-07-18 (Lane C):** claimed C-Q2.
- **2026-07-18 (Lane C, C-2 implementation ready):** `zing render <edl.json>` now validates the pinned contiguous timeline and media contract, authors static/karaoke-pop ASS through pysubs2, normalizes/concats every video leg, retains clip audio, places one-shot VO/music tracks, ducks music by about 9 dB under VO, and always emits 48 kHz stereo (silence when needed). The local content oracle and full suite are green: ffprobe metadata, red/blue midpoint pixels, generated-ASS inspection, caption-region frame delta, frequency-isolated duck depth, clip-audio unity, and silent-track checks. Caption length/line/CPS limits warn, and anchors remain inside the scaled universal safe box. Honest limitation: Arial is requested but no caption font asset is pinned yet, so exact glyph shape can vary with the host font provider; tests gate visible semantics rather than byte-identical frames.
- **2026-07-18 (Lane C):** claimed C-Q3.
- **2026-07-18 (Lane C, C-Q3 implementation ready):** live `--study` eval runs now time ingest, shots, keyframes, transcription, OCR, audio analysis, markdown, and a representative default-profile render. Report schema v2 preserves per-case measurements and aggregates per-stage totals, means, and maxima with duration, real-time factor, platform, CPU, and FFmpeg provenance. The ROADMAP's 30–60s target is recorded as `tracked-not-gated`; runs outside that range are marked non-comparable rather than assigned a misleading verdict. The full suite is green: 211 passed.
- **2026-07-18 (orchestrator):** C-1/C-2/C-Q3 all ACCEPTED — the content oracle (pixel probes, frequency-isolated duck depth, ASS inspection) is exactly the anti-slop discipline this repo is about; the tracked-not-gated perf verdict is the honest call. Retroactive AUTHORIZATION on record: Lane C owns the eval/render CI workflow steps in .github/workflows (ffmpeg install/probes, artifact retention) — you asked before acting and #24 shipped it green on both OSes; that ownership is now official. Font pinning: park as an S2 item (pin a redistributable OFL font for captions so glyphs are reproducible). NEXT: C-Q4 (real-video regression bootstrap — refetch the two EXAMPLE-DATASET videos by URL, run the adapter, freeze outputs + provenance vs D-Q2 truth), then your S1 review-round duty: review Lanes A+B merged code → handoff/reviews/S1-REVIEW-lane-c.md.
- **2026-07-18 (Lane C):** claimed C-Q4.
- **2026-07-18 (Lane C, C-Q4 implementation ready):** ran the default `large-v2` CPU study/render adapter against both canonical EXAMPLE-DATASET videos and froze portable JSON Breakdowns plus source, environment, artifact, human-truth, and stage-timing provenance under `tools/eval/real_videos/`. The 42.534s reference measured 23 shots, 146 words, and 47 caption events in 66.97s including render; the 55.074s “raw proxy” measured 34 shots, 178 words, and 49 caption events in 117.67s. This confirms Lane A's warning and contradicts D-Q2's zero-cut annotation: the published raw-proxy upload has 15 cuts in the 20–30s window. The fixture records that caveat instead of treating the annotation as scoring truth. Source media and extracted frames are not committed because redistribution terms are undocumented; source and normalized-media SHA-256 hashes make refetches comparable. The full offline suite is green: 214 passed.
- **2026-07-18 (Lane C, S1 cross-review ready):** reviewed merged Lane A+B code and recorded five ranked findings in `handoff/reviews/S1-REVIEW-lane-c.md`. Reproduced three release-significant boundary failures: MCP slug traversal can read and rewrite a Breakdown outside `ZING_HOME`; `get_breakdown` serves an old snapshot as ready during re-study; and doctor can report yt-dlp healthy when Lane A cannot invoke it. Also documented orphaned background-job state and unfinished X/long-form consumer contracts. The full suite remains green: 214 passed.
- **2026-07-18 (orchestrator): S1 FIX SPRINT OPEN.** Your items are in handoff/reviews/S1-FIXLIST.md (Lane A: F-06/07/08 + P3 share; Lane B: F-02 SECURITY first, then F-03/04/05/10/11/15; Lane C: F-01 CI first, then F-09/12/13/14). One fix per PR, regression test that fails before the fix, P1s before P2s. Nothing new until P1/P2 clear.
- **2026-07-18 (orchestrator):** post-fix-sprint queue seeded: C-Q5 (F-13/F-14 + landscape presets — long-form scope depends on it), C-Q6 (speech fixture, enable speech_ratio scoring), C-Q7 (loudness/TP report fields), C-Q8 (P3 tail sweep). Work in order. Note: F-09/F-12 in your files were fixed by orchestrator agents during the fix sprint (PRs #48-#50 window) — review those diffs as you go and flag anything you'd have done differently in your NOTES.
- **2026-07-18 (Lane C):** claimed C-Q5.
- **2026-07-18 (Lane C, overnight re-review):** reviewed the Lane C portions of PRs #45 and #50. I would keep both implementations as merged: the dedicated real-FFmpeg jobs make the cross-platform contract explicit, and the iterative monotonic matcher preserves the scorer's one-to-one semantics without the old recursion limit or fabricated distant cut pairs. The new adversarial and high-cardinality tests cover the failure modes I would have required. No corrective follow-up is needed.
- **2026-07-18 (Lane C, C-Q5 implementation ready):** the renderer now validates 9:16, 16:9, and 1:1 output presets; derives caption anchors and text scale from the selected EDL dimensions; and holds each word-pop event until the next word begins or the caption ends. A real 16:9 FFmpeg render verifies dimensions, horizontal centering, and visible caption content. The regressions failed against the prior implementation, and the full suite is green: 330 passed.
- **2026-07-18 (Lane C):** claimed C-Q6.
- **2026-07-18 (Lane C, C-Q6 implementation ready):** checked in a one-second, 16 kHz PCM excerpt from LibriVox's public-domain recording of Kate Chopin's “Ripe Figs,” with upstream and artifact hashes, reader attribution, territorial notice, `CC-PDDC` identifier, and a reproducible extraction command. Golden generation now concatenates that spoken clip with exact FFmpeg silence, records a constructed speech ratio of 0.667, and enables speech-ratio pass/fail in scorer v1.2.0. All three generated videos measure within 0.02 of truth under the project VAD. The new ratio mutation failed before enablement and now fails only the speech dimension. I also refreshed the two Lane C provenance hashes invalidated by PR #52's human-truth correction; PR #52's unit jobs were red on the stale hashes. The full suite is green: 332 passed.
- **2026-07-18 (Lane C, PROCESS OBSERVATION C-Q6):** hash-pinning the cross-lane human-truth file caught real provenance drift, but PR #52 merged while its unit jobs were red and broke every downstream branch. Recommendation: a PR that changes shared truth must update dependent snapshot hashes in the same merge, or stop at the red gate and explicitly hand the hash refresh to the owning lane before merge.
- **2026-07-18 (orchestrator, STANDING RULE — process retro):** whenever you finish a queue item, append a short PROCESS OBSERVATION to this file: what about this multi-agent process (specs, queues, NOTES, reviews, CI, orchestration) helped, hurt, or should change — one concrete recommendation each time. The orchestrator folds accepted ones into the process. Critical observations wanted, not praise.
- **2026-07-18 (Lane C):** claimed C-Q7.
- **2026-07-18 (Lane C, C-Q7 implementation ready):** eval report schema v3 now records per-case integrated LUFS and true peak from FFmpeg's `ebur128=peak=true`, plus an aggregate advisory summary. It warns outside [-18, -10] LUFS or above -1 dBTP, records a reason when media or a finite measurement is unavailable, and never changes the scorer verdict. A deliberately clipped real-audio regression produces both warnings while the case still passes. The full suite is green: 334 passed.
- **2026-07-18 (Lane C, PROCESS OBSERVATION C-Q7):** the queue's exact thresholds and explicit “warnings, not gates” boundary prevented a taste-research number from silently becoming a release gate. Recommendation: future report-field assignments should also pin the missing-data shape and whether the report schema must bump; both are compatibility decisions, not implementation trivia.
- **2026-07-18 (Lane C):** claimed C-Q8.
- **2026-07-18 (Lane C, C-Q8 implementation ready):** reread all three S1 review files and closed the remaining reproducible Lane C P3s: real-video freezes now derive platform from the canonical source URL; render publication falls back only on cross-filesystem move errors; and bare eval CLI runs explicitly identify the default checked-in sample mode in output and help. Separate regressions failed for all three before the fixes. C-Q5 and PR #50 already cover the other Lane C findings; overlapping word timings remain correctly assigned to upstream EDL sanitization, and F-15 remains the active Lane A/B workspace seam. The full suite is green: 338 passed.
- **2026-07-18 (Lane C, PROCESS OBSERVATION C-Q8):** rereading the source reviews, rather than relying only on the deduplicated fixlist, found three small Lane C items the fixlist had collapsed into F-17. Recommendation: when reviews are deduplicated, give every tail bullet an owner, stable ID, and status line so later sweeps can verify deltas without repeating a full unindexed audit.
- **2026-07-18 (Lane C):** claimed C-Q9 after verifying `docs/taste/TRANSITIONS-AND-MIX.md` is present on main.
- **2026-07-18 (Lane C, C-Q9 implementation ready):** added exact FFmpeg goldens for hard cuts, dissolves, wipes, zoom punches, and audio-aligned cuts, plus a dependency-free prototype that combines frame difference, histogram drift, block-matched radial flow, spatial-frontier motion, and audio-onset alignment. The report exposes per-signature precision and states that 1.0 precision on five synthetic fixtures says nothing about recall or real-video performance. The regression failed before implementation; focused tests and the rebased full suite are green at 346 passed.
- **2026-07-18 (Lane C, PROCESS OBSERVATION C-Q9):** the first audio-alignment calibration sampled an 880 Hz onset at 1 kHz and erased the signal through aliasing. Recommendation: every feature prototype should test its own measurement bandwidth against the generated fixture, and reports should distinguish synthetic calibration scores from corpus validation.
- **2026-07-18 (Lane C):** claimed C-Q10 after verifying `docs/taste/PACKAGING-INTROS-THUMBNAILS.md` is present on main. The queue explicitly assigns Lane C the CLI and MCP surfaces for this item.
- **2026-07-18 (Lane C, C-Q10 implementation ready):** `zing thumbs <video>` now selects 3–5 caption-free, cut-safe, perceptually distinct source frames from speech-window loudness peaks, title-term reveal boundaries, the hook window, and measured contrast/color/sharpness. It extracts source-resolution JPEGs, writes a portable manifest, and emits three distinct, literal-hook-grounded image-model prompts. The MCP tool returns the same JSON and attaches every candidate as an image block. The package states that it does not visually verify faces, emotions, object identity, semantic payoff, or CTR potential. The regression failed before implementation; the focused surface is green at 68 passed and the full suite at 352 passed.
- **2026-07-18 (Lane C, PROCESS OBSERVATION C-Q10):** the assignment named an MCP image surface while its referenced `get_frames` dependency remained only a Lane B design note. Recommendation: gated prototype entries should list required landed dependencies and state whether the queue item expands ownership across shared CLI/MCP routers; this avoids guessing at cross-lane seams.
- **2026-07-18 (Lane C):** claimed V-C after confirming `handoff/research/R3-grok-x-findings.md` has landed on main.
- **2026-07-18 (Lane C, V-C complete):** added `docs/taste/VIRALITY-X.md`, separating public post views from two-second video views; defining account-breakout, cohort-viral, and durable-viral tiers; mapping current For You retrieval/ranking and X video surfaces; specifying first-hour analytics, genre differences, and an explicitly experimental file-only rubric. The pre-draft regression failed on the missing deliverable; the required-section check now passes with eight distinct live sources.
- **2026-07-18 (Lane C, PROCESS OBSERVATION V-C):** the R3 Grok synthesis was unusually useful because it carried its own T3/T4 ceiling and a list of claims that lacked receipts; that made the primary-source verification boundary visible. Recommendation: every research handoff should include an “asserted without receipts” section so downstream briefs can reject attractive claims without repeating the entire provenance audit.
- **2026-07-18 (Lane C):** C-Q11 is already complete as V-C in PR #67; claimed C-Q12.
- **2026-07-18 (Lane C, C-Q12 schema request):** please add `TransitionObservation(kind: str, start: float, end: float, frame_pair_count: int = 0, audio_aligned: bool = False, audio_onset_delta: float | None = None)` and `Breakdown.transitions: list[TransitionObservation] = field(default_factory=list)`, with lossless `from_dict` support. Allowed `kind` values for v1 are `hard_cut`, `dissolve`, `wipe`, and `zoom_punch`; the fifth #65 signature is represented by `kind="hard_cut"` plus `audio_aligned=True` and the signed onset-minus-cut delta, avoiding a duplicate event. I am deliberately omitting confidence: #65 has synthetic per-signature precision, not calibrated per-event probabilities. Proposed opt-in surface after approval: `study(..., detect_transitions: bool = False)` and CLI `zing study ... --transitions`; failures return an empty list plus a named warning, and provenance records detector/version/thresholds. Please rule on whether this additive serialized field bumps `Breakdown.schema_version` to 2; Lane C will not edit `schemas.py`.
- **2026-07-18 (orchestrator RULING, C-Q12): APPROVED exactly as proposed** — TransitionObservation + Breakdown.transitions are live in schemas.py (your kind-v1 vocabulary, audio_aligned instead of a duplicate event kind, NO confidence field — your reasoning on that is now in the docstring). schema_version stays 1: policy is additive-with-default changes never bump (matching warnings/provenance/keyframe precedent); bump is reserved for breaking/semantic changes. Build the opt-in surface as proposed (study(detect_transitions=False), --transitions, empty-list+warning on failure, detector provenance). Your process observations C-Q6..V-C are all accepted — the shared-truth hash rule is going into ROADMAP standing rules; the rest shape future assignment style. Exceptional run today.
- **2026-07-18 (Lane C):** while C-Q12 was waiting on the requested orchestrator schema ruling in PR #68, claimed C-Q13 as unblocked Lane C work.
- **2026-07-18 (Lane C, C-Q13 implementation ready):** added measured fixtures for the 10:34 landscape Big Buck Bunny, an 18.282s native NASA Artemis X post, and the 60.118s Daily Dweebs short. A schema-v2 expansion manifest pins canonical sources, exact yt-dlp formats, expected media properties, rights evidence, acquisition hashes, environment, stage timings, and a hashed source register. It records that no independent human annotations were commissioned instead of implying accuracy truth. Source videos remain uncommitted; 174 downscaled, SHA-tracked analysis thumbnails keep the visual measurements inspectable. The regression failed when the old freezer rejected measurement-only cases; the focused suite is green at 6 passed and the full suite at 355 passed.
- **2026-07-18 (Lane C, PROCESS OBSERVATION C-Q13):** “full provenance” covered acquisition but did not say whether every new source needed human annotation, which could have encouraged fabricated truth or inconsistent fixture shapes. Recommendation: corpus queue items should separately require acquisition provenance, rights evidence, and annotation status before collection begins; unavailable annotation must be a valid explicit state.
- **2026-07-18 (Lane C, C-Q12 implementation ready):** promoted the #65 detector from the eval-only prototype into the installed study package and made the eval harness call that same implementation. `study(..., detect_transitions=False)` and `zing study --transitions` now produce typed hard-cut, dissolve, wipe, and zoom-punch observations; audio-aligned cuts remain one hard-cut event with a signed onset delta. Opt-in runs record detector name, version, thresholds, and feature summary. Probe failures persist an empty list, the detector contract, and a named warning. Disabled runs do no transition work. The API and CLI regressions failed before integration; 21 focused tests and the full 384-test suite pass.
- **2026-07-18 (Lane C, PROCESS OBSERVATION C-Q12):** the schema-first handshake prevented a cross-lane ownership violation, but the queue had no explicit blocked-on state while the next independent item was ready. Recommendation: schema-gated queue entries should name the prerequisite PR or owner and state that later independent items may proceed; the orchestrator’s approval note then becomes an unambiguous unblock signal.
- **2026-07-18 (orchestrator): SPRINT 2 IS OPEN** — handoff/SPRINT-2-D2.md. StyleProfile + StatSummary contracts are live in schemas.py. S2 lane items take priority over standing generators; S2-prep items already done fold in (transitions, get_frames, prompt pack 0.4.0 are the foundation). Same discipline as S1.
- **2026-07-18 (Lane C):** claimed S2-C profile eval.
- **2026-07-18 (Lane C, S2-C profile eval complete):** PR #93 merged at `b2376b3` after all six CI checks passed. Eval report schema v4 now scores all 11 measured StyleProfile dimensions exactly, with five constructed Breakdowns, an isolated mutation per aggregate family, and a five-video regression over the frozen real set. The regression first failed because the profile scorer did not exist; the rebased full suite passes at 446 tests, and `tools.eval.run --sample --profiles` passes both profile cases.
- **2026-07-18 (Lane C, PROCESS OBSERVATION S2-C):** the “once Lane A lands” gate had no machine-readable dependency state, so Lane C had to poll GitHub and another worktree while holding the downstream PR locally. Recommendation: cross-lane queue gates should name the upstream branch or PR and the exact landed symbol or file that clears the dependency; downstream lanes can then verify the unblock without reading another lane’s uncommitted tree.
- **2026-07-18 (orchestrator, PROTOCOL CHANGE — CI quota exhausted):** GitHub Actions refuses to start jobs (private-repo minutes gone; macOS 10x multiplier + today's volume). Until further notice: do NOT wait on checks (they will never run). REPLACEMENT GATE: run the FULL local suite with ffmpeg gates (ZING_REQUIRE_FFMPEG=1 python -m pytest) and paste the pass-count line into the PR body, then merge. Doc-only changes may merge with a stated 'doc-only' line instead. The discipline is the gate now — betray it and we are blind.
- **2026-07-18 (Lane C):** claimed C-Q14.
- **2026-07-18 (Lane C, C-Q14 implementation ready):** detector v3 now requires a gradual run to have at least 0.75 temporal monotonicity and 1.0 mean frame difference before calling it a dissolve. The gate suppresses all 29 detector-v2 candidates across the three frozen gate-pack sources while preserving the exact FFmpeg crossfade. The eval report keeps synthetic precision separate and records real-video precision as unavailable because the corpus has no exhaustive human transition labels. The non-monotonic-motion regression failed before the fix; five focused tests and the required full ffmpeg-gated suite pass at 454 tests.
- **2026-07-18 (Lane C, PROCESS OBSERVATION C-Q14):** perfect synthetic precision hid a fallback classifier that labeled every otherwise-unrecognized gradual run as a dissolve. Recommendation: every fallback classifier needs an explicit abstain path and a frozen real-data rejection check after its exact goldens; report rejection rate separately from precision when the real corpus is unjudged.
- **2026-07-19 (orchestrator): CI RESTORED (GitHub Pro) — local-gate mode retired.** Resume the normal flow: gh pr checks <n> --watch, merge only on all-green. Keep pasting the local suite line in PR bodies anyway — it proved its worth and costs nothing.
- **2026-07-19 (orchestrator): IDLE IS ABOLISHED for all lanes** — no unclaimed lane items means claim ONE standing generator (QUEUE.md bottom), rotating, every cycle. Lane C: C-Q15/C-Q16 are also freshly queued. ALSO: repo is now PUBLIC — unlimited Actions minutes, and TRUE AUTO-MERGE is restored: gh pr merge --auto --squash --delete-branch works again (branch protection now enforces all six checks, so red merges are impossible).
- **2026-07-19 (orchestrator): SPRINT 3 (DIRECT) IS OPEN — handoff/SPRINT-3-D3.md.** The anti-slop core. Naming/branding is parked by Ryan (research continues in background; build under existing names/codenames). S3 lane items take priority over generators.
- **2026-07-19 (Lane C):** claimed C-Q15.
- **2026-07-19 (Lane C, C-Q15 implementation ready):** added three exact FFmpeg raw-footage goldens: a 2.0-second dead-air span, a public-domain 0.25-second “like” insertion, and a byte-identical repeated take. Version 1.3.0 of the scorer measures dead-air timing, filler identity/timing, and repeated-take locations/similarity independently; one mutation per dimension fails only that dimension. The regression first failed because the raw-footage generator contract did not exist. After A-Q12 landed during rebase, a schema-free adapter was added for its internal `RawResult` shape. The focused surface passes at 28 tests and the rebased full ffmpeg-gated suite at 488 tests.
- **2026-07-19 (Lane C, PROCESS OBSERVATION C-Q15):** A-Q12 and C-Q15 were paired without a shared field contract or an explicit lead item, and Lane A’s half landed only when C-Q15 rebased. Recommendation: paired queue items should name the contract owner, stable field names, and whether the second item is gated; otherwise the second lane must start behind a dictionary seam and reconcile it before ready.
- **2026-07-19 (Lane C):** claimed Sprint 3 direction-format eval.
- **2026-07-19 (Lane C, Sprint 3 direction-format eval ready):** added a format-only validator for `judgment["direct"]`: required top-level and nested fields, exact criterion-ID membership against `docs/taste/INDEX.md`, and conservative shot-prompt checks for more than two sentences or review-maintained production jargon. Eval report schema v5 records the validator, criterion-index, jargon-list, and fixture hashes; `--directions` runs the checked-in conforming output. Eight isolated mutations each fail only their intended dimension. The regression first failed because the validator module did not exist; a second red audit caught valid mixed-case index IDs before the parser shipped. The rebased full FFmpeg-gated suite passes at 503 tests.
- **2026-07-19 (Lane C, PROCESS OBSERVATION Sprint 3 direction-format eval):** the generated taste index is the validator’s contract, but it has no machine-readable sidecar, contains mixed-case IDs, and repeats `U1`, `U3`, `U4`, and `U5`. Recommendation: have the index generator emit a versioned JSON ID set and fail on duplicate IDs; consumers should not need to infer an identifier grammar from a Markdown table.
- **2026-07-19 (Lane C):** claimed C-Q16.
- **2026-07-19 (Lane C, C-Q16 implementation ready):** transition report schema v3 now measures case-level synthetic recall and false negatives as well as precision; every constructed signature has 1.0 recall. A source-identity-pinned audit covers all five frozen real-video fixtures and all three gate-pack videos. None has exhaustive exact transition events and types, so real-video recall remains honestly unavailable. Detector v4 was rerun on the three SHA-matched gate-pack files and emitted five hard cuts, three wipes, one zoom punch, and no dissolves; those counts are recorded as unjudged execution evidence, not accuracy. Production opt-in results now carry an experimental validation status and warning until labeled real recall exists. The regression first failed because recall/audit support did not exist; a second red check required current-detector execution evidence. The full FFmpeg-gated suite passes at 507 tests.
- **2026-07-19 (Lane C, PROCESS OBSERVATION C-Q16):** the queue required a real-video recall number, but neither named corpus had exhaustive event labels; detector runs alone cannot reveal false negatives. Recommendation: assignments that require precision or recall should name the labeled truth artifact and annotation unit, or explicitly budget annotation work, before implementation starts.
- **2026-07-19 (Lane C):** claimed SG-1 cross-review of PRs #110, #112, and #113.
- **2026-07-19 (Lane C, SG-1 review complete):** reviewed the actual diffs for #110, #112, and #113. **P1 honesty finding for Lane A, `src/myzing/study/raw.py:196` and `:243`:** `measure_raw()` still derives a keeper when VAD and loudness are unavailable, then gives the judging AI the evidence “no interior dead air.” Reproduction: six clean synthetic words plus `speech_segments=None` and an empty loudness curve returns keeper `0.0-2.3s` alongside the warning that dead-air measurement was skipped. Lane A should skip keeper derivation, or mark it unavailable, whenever the measurements required by its definition are unavailable. #112’s internal `audio_probe_ok` distinction is correct, but the production wrapper discarded it; Lane C fixed that owned seam so `detect_transitions()` now persists `transition_audio_probe_ok` and names an audio-alignment skip. The regression failed on the missing production field before the fix. #113 is a findings-only note with no executable diff. The full FFmpeg-gated suite passes at 508 tests.
- **2026-07-19 (Lane C, PROCESS OBSERVATION SG-1):** #112’s PR body said the audio-probe honesty gap was closed after adding an internal flag, but no test followed that flag into the public `Breakdown` warnings/provenance surface. Recommendation: cross-review fixes for degraded states should prove the state at the first consumer-visible artifact, not only at the helper return value.
- **2026-07-19 (Lane C):** claimed SG-2 coverage sweep after confirming no numbered Lane C item was available.
- **2026-07-19 (Lane C, SG-2 coverage sweep complete):** the full existing suite measured `tools/eval/backfill_frames.py` as Lane C’s lowest-covered module at 58% branch coverage (lane total 82%). Added a successful CLI-path test and a missing-media regression, which first failed because the script rewrote its manifest before reporting the missing input. The CLI now preflights every fixture, media file, and truth document before its first write. The full FFmpeg-gated suite passes at 510 tests; module coverage is 83% and lane coverage is 83%.
- **2026-07-19 (Lane C, PROCESS OBSERVATION SG-2):** SG-2 requires a coverage increase but does not pin the source set, test set, or branch-coverage mode, so different cycles can report incomparable percentages. Recommendation: check in one lane-aware coverage command or configuration and require both the module and lane totals from that fixed denominator.
- **2026-07-19 (Lane C):** claimed SG-3 simplification pass after confirming no numbered Lane C item was available.
- **2026-07-19 (Lane C, SG-3 simplification complete):** success and failure eval reports now get their seven identical schema, scorer, manifest, timestamp, platform, Python, and FFmpeg fields from one `_report_environment()` builder. The structural regression failed before the cleanup because that shared seam did not exist, then proved both report paths consume it. Existing report assertions remain unchanged, and the full FFmpeg-gated suite passes at 511 tests.
- **2026-07-19 (Lane C, PROCESS OBSERVATION SG-3):** the universal “regression fails before the fix” rule conflicts with SG-3’s “no behavior change” requirement and pushes refactor PRs toward tests of private structure. Recommendation: require a red behavior regression for fixes, but for simplification require green characterization tests before the edit plus a stated duplication or complexity delta; that protects behavior without freezing implementation details.
- **2026-07-19 (orchestrator): LAUNCH MODE + SPRINT 4 OPEN** — Ryan's directive: build EVERYTHING fully, one major launch, he tests at launch. All Ryan-gates now internal. handoff/SPRINT-4-D4.md has your Track 1 (Assemble) + Track 2 (Taste Onboarding — preset packs like ai-tech-talking-head, viral-tiktok; zing setup flow) items. S3's open items finish first; then S4. Same discipline, no pauses.
- **2026-07-19 (Lane C):** claimed SG-4 trending-OSS scan after confirming no numbered Lane C item was available.
- **2026-07-19 (Lane C, SG-4 trending-OSS scan complete):** evaluated five previously unlisted video-creation repositories with current license, release, and activity evidence. AutoSubs is the strongest caption-workflow reference; OpenCut is worth watching for one-core/many-surfaces product architecture; premiere-agent is concepts-only because it has no license; PyAV does not beat Zing’s current FFmpeg subprocess boundary; and Mediabunny’s MPL-2.0 browser stack stays design-only. The coverage check first failed on all five missing entries, then passed with exactly one new heading per repository. The rebased full FFmpeg-gated suite passes at 513 tests.
- **2026-07-19 (Lane C, PROCESS OBSERVATION SG-4):** the prior-art ledger now contains repeated repositories and uneven health fields across lane scans, which makes freshness and deduplication manual. Recommendation: generate the prose appendix from a small versioned registry containing repository full name, checked-at date, license, pushed-at timestamp, release, and verdict; reject duplicate full names before merge.
- **2026-07-19 (Lane C):** claimed Sprint 4 Track 1 Assemble: local TTS provider/default, OpenTimelineIO export, and VO-aware render-quality probes.
- **2026-07-19 (Lane C, Sprint 4 Track 1 implementation ready):** added a reusable TTS provider protocol, a lazy local Kokoro ONNX adapter with explicit user-supplied model assets, and an assembly API/`zing render` options that turn UTF-8 scripts into ordinary schema-valid voiceover tracks without mutating the input EDL. Native `.otio` export preserves picture trims, source audio, independently overlapping VO/music tracks, gain/duck metadata, and caption markers. The content oracle proves generated VO and burned captions survive the same real FFmpeg render. CI now installs OpenTimelineIO and requires a native write/read round trip on all three operating systems. The regressions first failed because none of the three Assemble modules existed. The focused renderer surface passes at 47 tests with the optional local adapter skipped; a clean Python 3.12 environment passes the real OpenTimelineIO 0.18.1 round trip; and the rebased full FFmpeg-gated suite passes at 532 tests with that same local-only skip.
- **2026-07-19 (Lane C, Sprint 4 Track 1 packaging handoff):** [`kokoro-onnx` 0.5.0](https://github.com/thewh1teagle/kokoro-onnx/blob/main/pyproject.toml) currently requires Python <3.14 and makes `espeakng-loader` a mandatory dependency; [OpenTimelineIO 0.18.1](https://pypi.org/project/OpenTimelineIO/0.18.1/) has no CPython 3.14 wheel. Because `pyproject.toml` is outside Lane C’s owned paths and the sprint explicitly keeps espeak-ng out of the default install, the renderer imports both runtimes lazily and never downloads models. The package owner should expose them only as explicit optional extras with compatible Python markers, not add either to the base install.
- **2026-07-19 (Lane C, PROCESS OBSERVATION Sprint 4 Track 1):** the accepted prior-art note describes espeak-ng as an optional Kokoro fallback, but the selected `kokoro-onnx` release now pulls `espeakng-loader` unconditionally and excludes the repository’s supported Python 3.14 runtime. Recommendation: every dependency-naming sprint item should pin a checked version, supported-Python range, transitive license snapshot, and package-manifest owner at sprint open; “default” should distinguish the chosen provider from the base-install dependency set.
