{
  "schema_version": 1,
  "scorer_version": "1.1.0",
  "scorer_changelog": {
    "1.1.0": "F-09/F-12: leftover cuts are never positionally zipped into fabricated out-of-tolerance matches — a pair requires a delta within out_of_tolerance_pairing_window_seconds, everything else is reported missing/extra; matcher rewritten iteratively for real-video event counts. Pass/fail semantics unchanged; per-event match lists differ from 1.0.0.",
    "1.0.0": "Initial S1 scorer."
  },
  "cuts": {
    "count_exact": true,
    "time_tolerance_seconds": 0.15,
    "out_of_tolerance_pairing_window_seconds": 0.3,
    "matching": "maximum-cardinality one-to-one chronological; leftovers pair as out-of-tolerance matches only within the pairing window, otherwise they are reported as missing/extra, never paired"
  },
  "captions": {
    "minimum_text_similarity": 0.8,
    "matching": "positive temporal overlap, one-to-one chronological",
    "normalization": "Unicode NFKC, casefold, punctuation removal, whitespace collapse",
    "require_full_recall": true,
    "penalize_extras": true
  },
  "audio": {
    "signal_floor_dbfs": -50.0,
    "window_pattern_exact": true,
    "speech_ratio": {
      "enabled": false,
      "tolerance": 0.1,
      "reason": "Unavailable until a redistributable spoken fixture is approved; tone is not speech."
    }
  }
}
